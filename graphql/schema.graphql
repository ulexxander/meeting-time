scalar Time

type Organization {
  id: ID!
  name: String!
  createdAt: Time!
  updatedAt: Time
}

type Query {
  organizationByID(id: ID!): Organization
}

input OrganizationCreate {
  name: String!
}

type Mutation {
  createOrganization(input: OrganizationCreate!): ID!
}
