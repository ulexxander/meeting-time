scalar Time

type Team {
  id: ID!
  name: String!
  createdAt: Time!
  updatedAt: Time
}

type Query {
  teamByID(id: ID!): Team
}

input TeamCreate {
  name: String!
}

type Mutation {
  teamCreate(input: TeamCreate!): ID!
}
